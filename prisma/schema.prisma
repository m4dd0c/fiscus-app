generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  username @unique String
  id       @id @default(autoIncreament)
  email    String @unique
  password? String
  access_token? String
  fundingUrl? String
}
